{
  "name": "aiteam-bundles",
  "version": "0.1.0",
  "private": true,
  "description": "Public catalog of AITEAM-X agent bundles. Consumed by the AITEAM-X dashboard via /api/bundles/catalog.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "check-catalog": "node scripts/check-catalog.mjs",
    "test": "npm run validate && npm run check-catalog"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "gray-matter": "^4.0.3"
  }
}
